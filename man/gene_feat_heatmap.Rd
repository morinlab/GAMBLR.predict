% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_feat_heatmap.R
\name{gene_feat_heatmap}
\alias{gene_feat_heatmap}
\title{Gene Feature Association Heatmap}
\usage{
gene_feat_heatmap(
  gene,
  all_feats,
  glm_results,
  metadata = NULL,
  metadata_columns = "pathology",
  sort_by_columns = NULL,
  strip_gene_prefix = NULL,
  drop_uninformative = TRUE,
  sort_features_by = NULL,
  column_split_genes = NULL,
  show_annotation_legend = TRUE,
  show_row_names = FALSE,
  cluster_columns = TRUE,
  cluster_rows = TRUE,
  ...
)
}
\arguments{
\item{gene}{Character scalar or vector of gene symbols. Used as column
prefixes in \code{all_feats} (e.g. \code{"MYD88"} matches \code{MYD88_L265P},
\code{MYD88_hotspot}, etc.). When multiple genes are supplied the heatmap
columns are split by gene by default (see \code{column_split_genes}).}

\item{all_feats}{Data frame or matrix whose columns are features (\verb{GENE_*})
and whose rows are samples. Typically the output of
\code{GAMBLR.utils::summarize_mutation_status()}.}

\item{glm_results}{Data frame returned by
\link[GAMBLR.utils:select_informative_features]{GAMBLR.utils::select_informative_features}. Must contain at least the
columns \code{feature}, \code{coef}, and \code{comparison}.}

\item{metadata}{Optional data frame with sample-level metadata. When
supplied, one or more left-side row annotations are added using the
columns named in \code{metadata_columns}. Sample matching uses the
\code{sample_id} column if present, otherwise row names.}

\item{metadata_columns}{Character vector of column names in \code{metadata} to
show as row annotations. Colours are assigned automatically via
\code{\link[GAMBLR.viz:map_metadata_to_colours]{GAMBLR.viz::map_metadata_to_colours()}}. Default \code{"pathology"}.}

\item{sort_by_columns}{Optional character vector of names to sort heatmap
rows by, in priority order. Each name is resolved in turn: (1) as a
feature matrix column (after prefix stripping) — sorted descending so
mutated samples appear first; (2) as a \code{metadata} column name — sorted
ascending; (3) as a value present in any metadata column (e.g. \code{"FL"}
found in \code{pathology}) — sorted so matching samples appear first. Any mix
is valid. Overrides \code{cluster_rows}. Default \code{NULL} (use clustering).}

\item{strip_gene_prefix}{Logical. If \code{TRUE}, removes the leading \code{GENE_}
prefix from column labels. Defaults to \code{TRUE} for a single gene and
\code{FALSE} for multiple genes (where full names are needed to distinguish
features with the same suffix across genes).}

\item{drop_uninformative}{Logical. If \code{TRUE} (default), features with a
zero coefficient in every comparison are dropped and a warning is issued.
Set to \code{FALSE} to retain all features.}

\item{sort_features_by}{Optional character vector of comparison names
(after \code{"vs rest"} trimming, e.g. \code{c("DLBCL", "FL")}). Columns are
sorted by descending coefficient for the first group, ties broken by the
second, and so on. When \code{column_split_genes = TRUE} the sort is applied
independently within each gene block; otherwise it is global. Overrides
\code{cluster_columns}. Default \code{NULL} (use clustering).}

\item{column_split_genes}{Logical. When multiple genes are supplied,
whether to use ComplexHeatmap's \code{column_split} to visually group features
by gene. Defaults to \code{TRUE} for multiple genes, \code{FALSE} for one.}

\item{show_annotation_legend}{Logical. If \code{TRUE} (default), a single colour
legend for the coefficient scale is shown (attached to the first
comparison track). Set to \code{FALSE} to suppress it entirely.}

\item{show_row_names}{Logical. Show sample (row) names. Default \code{FALSE}.}

\item{cluster_columns}{Logical. Cluster heatmap columns. Default \code{TRUE}.}

\item{cluster_rows}{Logical. Cluster heatmap rows. Default \code{TRUE}.}

\item{...}{Additional arguments passed directly to
\code{\link[ComplexHeatmap:Heatmap]{ComplexHeatmap::Heatmap()}}, e.g. \code{column_title}, \code{row_split},
\code{rect_gp}.}
}
\value{
A \link[ComplexHeatmap:Heatmap-class]{ComplexHeatmap::Heatmap} object.
}
\description{
Visualise one or more genes' binary/count feature matrix
alongside the coefficients returned by
\link[GAMBLR.utils:select_informative_features]{GAMBLR.utils::select_informative_features}. Each column of the heatmap is
one feature (e.g. \code{MYD88_L265P}, \code{EZH2_Y641}); each row is a sample with
at least one non-zero value across all queried genes. Column annotations
below show the glmnet coefficient for every comparison, using a diverging
blue–white–red scale centred at zero. When multiple genes are supplied a
gene-identity track is prepended to those annotations.
}
\examples{
\dontrun{
glm_res <- select_informative_features(feature_df, meta)

# single gene
gene_feat_heatmap("MYD88", feature_df, glm_res)

# multiple genes, features split by gene
gene_feat_heatmap(c("MYD88", "EZH2"), feature_df, glm_res)
}
}
