% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_interactive_umap_scatterplot.R
\name{make_interactive_umap_scatterplot}
\alias{make_interactive_umap_scatterplot}
\title{Interactive UMAP Scatterplot With Feature Hover}
\usage{
make_interactive_umap_scatterplot(
  umap_result,
  sample_id_column = "sample_id",
  drop_composite = TRUE,
  colour_by = "lymphgen",
  drop_other = FALSE,
  high_confidence = FALSE,
  custom_colours,
  title = NULL,
  alpha = 0.8,
  point_size = 5,
  max_features = 30
)
}
\arguments{
\item{umap_result}{The list returned by \code{make_and_annotate_umap()}. Must
contain a \verb{$df} element (UMAP coordinates and metadata, with columns \code{V1}
and \code{V2}) and a \verb{$features} element (the sample-by-feature matrix used to
build the UMAP, with sample IDs as row names).}

\item{sample_id_column}{Name of the column in \code{umap_result$df} that holds
sample identifiers, used to match rows in \verb{$features}. Default
\code{"sample_id"}.}

\item{drop_composite}{Logical. If \code{TRUE}, removes samples whose
\code{colour_by} value contains \code{"COMP"}. Default \code{TRUE}.}

\item{colour_by}{Name of the column in \code{umap_result$df} used to colour
points. Default \code{"lymphgen"}.}

\item{drop_other}{Logical. If \code{TRUE}, removes samples with \code{colour_by}
equal to \code{"Other"} or \code{"NOS"}. Default \code{FALSE}.}

\item{high_confidence}{Logical. If \code{TRUE}, retains only samples with
\code{Confidence > 0.7}. Default \code{FALSE}.}

\item{custom_colours}{Optional named character vector mapping group labels
to hex colours. Defaults to \code{\link[GAMBLR.helpers:get_gambl_colours]{GAMBLR.helpers::get_gambl_colours()}}.}

\item{title}{Optional plot title string.}

\item{alpha}{Point opacity (0–1). Default \code{0.8}.}

\item{point_size}{Point diameter in pixels. Default \code{5}.}

\item{max_features}{Maximum number of feature names to show per sample in
the hover tooltip. Features beyond this limit are summarised as
\code{"... +N more"}. Default \code{30}.}
}
\value{
A \code{plotly} htmlwidget.
}
\description{
Generates an interactive plotly scatterplot from the output of
\code{make_and_annotate_umap()}, mirroring the aesthetics of
\link{make_umap_scatterplot}. Hovering over any point displays
all non-zero (mutated) features for that sample, drawn from the feature
matrix stored in the result object.
}
\examples{
\dontrun{
my_umap <- make_and_annotate_umap(feature_df, metadata)

make_interactive_umap_scatterplot(
  umap_result = my_umap,
  colour_by   = "lymphgen",
  drop_other  = TRUE
)
}
}
